# C++ 算法笔试必记语法与函数手册

> 面试 / 笔试速查手册。所有代码可直接复制使用。

## 必备头文件

```cpp
#include <iostream>
#include <string>
#include <vector>
#include <queue>
#include <stack>
#include <deque>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>
#include <algorithm>
#include <numeric>   // accumulate
#include <cstdio>    // getchar（快读）
#include <cmath>
#include <cstring>
using namespace std;
```

---

## 一、string 字符串操作

### 1. 拼接操作

```cpp
string s1 = "abc", s2 = "123";
string res = s1 + s2;      // 直接+拼接，结果: "abc123"
s1.append(s2);             // 追加字符串，s1变为 "abc123"
s1.push_back('d');         // 追加单个字符，s1变为 "abc123d"
```

### 2. 查找操作（重点）

```cpp
string s = "hello algorithm";
size_t pos = s.find("alg");   // 找到返回起始下标(此处为6)，找不到返回 string::npos
if (pos != string::npos) {
    // 匹配成功的逻辑
}

size_t rpos = s.rfind("o");              // 从后往前查找，返回下标4
size_t fpos = s.find_first_of("aeiou");  // 查找第一个匹配字符，返回1
size_t lpos = s.find_last_of("aeiou");   // 查找最后一个匹配字符
```

### 3. 截取、删除、替换

```cpp
string sub = s.substr(6, 5);      // 从下标6开始，取5个字符，结果 "algor"
s.erase(6, 5);                    // 从下标6开始删除5个字符
s.replace(6, 5, "code");          // 从下标6开始，5个字符替换为"code"
```

### 4. 其他常用

```cpp
int len = s.size();               // 字符串长度，等价于 s.length()
bool isEmpty = s.empty();         // 判断是否为空
char c = s[0];                    // 按下标访问字符
int num = stoi("1234");           // 字符串转int
string str = to_string(1234);     // 数字转字符串
const char* p = s.c_str();        // 转为const char* 类型
```

---

## 二、getline 整行读取（重点坑点）

```cpp
string line;
getline(cin, line);               // 读取一整行字符串，包含空格

// ⚠️ 经典坑：cin >> 后残留换行符，getline会读到空行
// 正确写法：用 cin.ignore() 吃掉缓冲区的换行
int n;
cin >> n;
cin.ignore();       // 必须！清除cin留下的换行符
getline(cin, line); // 正常读取一整行内容
```

---

## 三、STL 容器常用语法速查

### 1. vector 动态数组（最常用）

```cpp
vector<int> v;
vector<int> v2(10, 0);      // 初始化10个元素，值全为0

v.push_back(5);             // 尾部插入元素
v.pop_back();               // 删除尾部元素
int first = v[0];           // 下标访问
int sz = v.size();
bool emp = v.empty();
v.clear();                  // 清空所有元素

// 遍历方式
for (int i = 0; i < v.size(); i++) { cout << v[i]; }
for (int x : v) { cout << x; } // 范围for
```

### 2. deque 双端队列

```cpp
deque<int> dq;
dq.push_back(1);    // 尾部插入
dq.push_front(2);   // 头部插入
dq.pop_back();      // 尾部删除
dq.pop_front();     // 头部删除
int f = dq.front(); // 队首元素
int b = dq.back();  // 队尾元素
```

### 3. stack 栈

```cpp
stack<int> st;
st.push(10);        // 入栈
st.pop();           // 出栈（无返回值）
int top = st.top(); // 取栈顶元素
st.empty();
st.size();
```

### 4. queue 普通队列

```cpp
queue<int> q;
q.push(10);         // 入队
q.pop();            // 出队（无返回值）
int front = q.front(); // 队首元素
int back = q.back();   // 队尾元素
q.empty();
```

### 5. priority_queue 优先队列（堆）

```cpp
priority_queue<int> maxHeap;      // 默认大根堆
priority_queue<int, vector<int>, greater<int>> minHeap; // 小根堆

maxHeap.push(20);
maxHeap.pop();
int topVal = maxHeap.top();
```

### 6. set 有序集合（红黑树，自动去重+升序）

```cpp
set<int> s;
s.insert(3);         // 插入元素
s.erase(3);          // 删除元素
int cnt = s.count(3); // 存在返回1，不存在返回0
auto it = s.find(3); // 查找，返回迭代器
```

### 7. unordered_set 哈希集合（无序，查找O(1)）

```cpp
unordered_set<int> us;
us.insert(5);
us.erase(5);
us.count(5);
```

### 8. map 有序映射（key自动升序，红黑树）

```cpp
map<int, string> mp;
mp[1] = "one";        // 插入/修改键值对
mp.insert({2, "two"});
mp.erase(1);          // 删除key为1的项
mp.count(1);          // 判断key是否存在
```

### 9. unordered_map 哈希映射（查找O(1)，最常用）

```cpp
unordered_map<string, int> ump;
ump["apple"] = 5;
ump.count("apple");
```

---

## 四、常用算法函数（algorithm 头文件）

### 1. sort 排序（重点）

```cpp
vector<int> arr = {3, 1, 4, 2, 5};
sort(arr.begin(), arr.end());                  // 默认升序：1 2 3 4 5
sort(arr.begin(), arr.end(), greater<int>());  // 降序：5 4 3 2 1

// 自定义比较函数示例
sort(arr.begin(), arr.end(), [](const auto &a, const auto &b){
    return a > b; // 返回true表示a排在b前面
});
```

### 2. 其他高频算法

```cpp
reverse(arr.begin(), arr.end()); // 反转区间元素
swap(a, b);                      // 交换两个变量
int maxVal = max(a, b);
int minVal = min(a, b);
auto it = unique(arr.begin(), arr.end()); // 去重（需先排序），返回去重后尾迭代器
arr.erase(it, arr.end());                // 删除重复元素
```

### 3. 二分查找 lower_bound / upper_bound（重点，需先排序）

- `lower_bound`: 第一个 **>= val** 的元素位置（迭代器）
- `upper_bound`: 第一个 **> val** 的元素位置（迭代器）
- 找不到时返回 `end()`，用 `it - v.begin()` 转下标

```cpp
vector<int> v = {1, 2, 4, 4, 6};
auto it1 = lower_bound(v.begin(), v.end(), 4); // 指向第一个4，下标2
auto it2 = upper_bound(v.begin(), v.end(), 4); // 指向6，下标4
int idx = it1 - v.begin();                     // 下标 = 2
if (it1 == v.end()) { /* 不存在 >= val 的元素 */ }

// 经典用法：统计 val 的出现次数 = upper_bound - lower_bound
int cnt = upper_bound(v.begin(), v.end(), 4) - lower_bound(v.begin(), v.end(), 4);

// set/map 用成员函数版（内部是红黑树，O(log n)）
auto sit = s.lower_bound(3);   // set中第一个 >= 3 的元素
auto mit = mp.lower_bound(3);  // map中第一个 key >= 3 的迭代器
```

### 4. 其他常用 algorithm 函数

```cpp
bool has = binary_search(v.begin(), v.end(), 4); // 判断是否存在，返回bool，需有序
int mx = *max_element(v.begin(), v.end());       // 最大值
int mxIdx = max_element(v.begin(), v.end()) - v.begin(); // 最大值下标
int mn = *min_element(v.begin(), v.end());       // 最小值
int sum = accumulate(v.begin(), v.end(), 0);     // 求和，初始值0（需 <numeric>）
int c = count(v.begin(), v.end(), 4);            // 统计值出现次数
auto fit = find(v.begin(), v.end(), 4);          // 找第一个==val的位置，找不到返回end()
```

### 5. sort 配合 lambda 对 pair / 结构体排序（贪心题高频）

```cpp
vector<pair<int, int>> pts;
sort(pts.begin(), pts.end(), [](pair<int, int> a, pair<int, int> b) {
    return a.second < b.second; // 按第二个元素升序；两个都按: 先比first再比second
});
sort(arr.begin(), arr.end(), [](int a, int b) { return a > b; }); // 等价降序
```

### 6. 全排列 next_permutation（需先sort到最小排列，配合do-while）

```cpp
sort(arr.begin(), arr.end());
do {
    // 处理当前排列 arr
} while (next_permutation(arr.begin(), arr.end())); // 无下一个排列时返回false
// prev_permutation(arr.begin(), arr.end()) 生成上一个排列
```

---

## 五、快速幂（n次幂快速运算，带取模）

> 计算 `(a^b) % mod`，时间复杂度 O(log b)

```cpp
long long qpow(long long a, long long b, long long mod) {
    long long res = 1;
    a %= mod;
    while (b > 0) {
        if (b & 1) { // 二进制最低位为1
            res = res * a % mod;
        }
        a = a * a % mod;
        b >>= 1; // 右移一位，等价于b /= 2
    }
    return res;
}

// 调用示例：计算 2^10 % 1000 = 24
// long long ans = qpow(2, 10, 1000);
```

---

## 六、素数快速判断函数

> 优化试除法，时间复杂度 O(sqrt(n))

```cpp
bool isPrime(int n) {
    if (n <= 1) return false;
    if (n == 2) return true;
    if (n % 2 == 0) return false; // 先排除所有偶数
    // 只遍历奇数，到sqrt(n)即可
    for (int i = 3; i <= sqrt(n); i += 2) {
        if (n % i == 0) return false;
    }
    return true;
}

// 调用示例：
// if (isPrime(17)) { ... }
```

---

## 七、补充常用工具函数

```cpp
// 1. 最大公约数（GCD）
int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}
// 也可直接用 __gcd(a,b) （需<algorithm>头文件）

// 2. 最小公倍数（LCM）
int lcm(int a, int b) {
    return a / gcd(a, b) * b;
}

// 3. 数组批量初始化（memset，按字节赋值）
int arr[100];
memset(arr, 0, sizeof(arr));   // 全部置0
memset(arr, -1, sizeof(arr));  // 全部置-1

// 4. 字符大小写转换
char c = 'A';
c = tolower(c); // 转小写 'a'
c = toupper(c); // 转大写 'A'
```

---

## 八、前缀和与差分（区间和 / 区间加高频）

### 1. 一维前缀和（下标从1开始，避免边界判断）

```cpp
vector<int> a(n + 1);
vector<int> pre(n + 1, 0);
for (int i = 1; i <= n; i++) pre[i] = pre[i - 1] + a[i];
// 区间 [l, r] 的和 = pre[r] - pre[l - 1]（O(1)查询）
```

### 2. 二维前缀和（子矩阵和，高频）

```cpp
int a[505][505], sum[505][505];
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        sum[i][j] = sum[i - 1][j] + sum[i][j - 1] - sum[i - 1][j - 1] + a[i][j];
// 子矩阵 (x1,y1) 到 (x2,y2) 的和：
//   ans = sum[x2][y2] - sum[x1-1][y2] - sum[x2][y1-1] + sum[x1-1][y1-1]
```

### 3. 差分数组（区间整体加，O(1)修改，最后前缀和还原）

```cpp
vector<int> diff(n + 2, 0);
// 区间 [l, r] 全部加 x：
diff[l] += x;
diff[r + 1] -= x;
// 所有修改完成后，对 diff 求前缀和还原成原数组
for (int i = 1; i <= n; i++) diff[i] += diff[i - 1];
```

---

## 九、位运算（高频技巧）

```cpp
int x = 12; // 二进制 1100
int c1 = __builtin_popcount(x);   // 二进制中1的个数 = 2
long long y = 12LL;
int c2 = __builtin_popcountll(y); // long long版
int k = __builtin_ctz(x);         // 末尾连续0个数 = 2（即最低位1在第2位）

// 判断第 k 位是否为1（从0开始）
bool isOne = (x >> k) & 1;
// 取最低位的1（lowbit，树状数组/状态压缩常用）
int lowbit = x & (-x);            // 12 & -12 = 4 (100)

// 常见位运算
x <<= 1;    // 左移1位 = x * 2
x >>= 1;    // 右移1位 = x / 2
int andV = a & b;   // 与
int orV  = a | b;   // 或
int xorV = a ^ b;   // 异或（相同为0，不同为1）
int notV = ~a;      // 取反
// 判断 x 是否是2的幂： x > 0 && (x & (x - 1)) == 0
```

---

## 十、读入优化（快读，大量输入时用）

> 需 `#include <cstdio>`。比 `cin` 快很多

```cpp
inline int read() {
    int x = 0, f = 1;
    char ch = getchar();
    while (ch < '0' || ch > '9') { // 跳过空白和非数字
        if (ch == '-') f = -1;
        ch = getchar();
    }
    while (ch >= '0' && ch <= '9') {
        x = x * 10 + ch - '0';
        ch = getchar();
    }
    return x * f;
}
// 用法： int n = read();
// long long 版把 int 改成 long long 即可
```

---

## 十一、DFS 与 BFS 模板（最高频）

### 1. DFS 回溯模板（子集/排列/组合/棋盘类通用）

> 核心三件套：**标记 -> 递归 -> 撤销**

```cpp
int n, arr[25];
bool vis[25];
vector<int> path;
void dfs(int pos) {
    if (pos == n) { // 找到一个完整答案
        // 处理 path
        return;
    }
    for (int i = 0; i < n; i++) {
        if (vis[i]) continue;   // 剪枝：已用过
        vis[i] = true;
        path.push_back(arr[i]);
        dfs(pos + 1);
        path.pop_back();        // 回溯：撤销选择
        vis[i] = false;
    }
}
```

### 2. BFS 网格模板（求最短步数，无权图最短路）

> 注：`auto [x, y]` 结构化绑定需 C++17（牛客/力扣均支持）

```cpp
int dx[4] = {1, -1, 0, 0};
int dy[4] = {0, 0, 1, -1};
int dist[505][505];            // 初始全 -1，-1 表示未访问
queue<pair<int, int>> q;
dist[sx][sy] = 0;
q.push({sx, sy});
while (!q.empty()) {
    auto [x, y] = q.front(); q.pop();
    for (int i = 0; i < 4; i++) {
        int nx = x + dx[i], ny = y + dy[i];
        if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue; // 越界
        if (dist[nx][ny] != -1 || 障碍) continue;             // 已访问或撞墙
        dist[nx][ny] = dist[x][y] + 1;
        q.push({nx, ny});
    }
}
// 答案：dist[ex][ey]（仍为-1则不可达）
```

---

## 十二、并查集（Union-Find，高频数据结构）

```cpp
int fa[100005];
// 初始化：每个点自成集合
void init(int n) { for (int i = 1; i <= n; i++) fa[i] = i; }
// 查：找根节点（路径压缩）
int find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }
// 合：两集合合并
void merge(int a, int b) {
    int ra = find(a), rb = find(b);
    if (ra != rb) fa[ra] = rb;
}
// 判断是否同一集合
bool same(int a, int b) { return find(a) == find(b); }
```

---

## 十三、图论：邻接表 / Dijkstra / 拓扑排序

### 1. 邻接表建图（带权）

```cpp
vector<pair<int, int>> g[100005]; // g[u] 存 {邻点v, 边权w}
// 无向图：g[u].push_back({v, w}); g[v].push_back({u, w});
```

### 2. Dijkstra 单源最短路（优先队列版，O(m log n)，边权非负）

```cpp
const int INF = 1e9;
vector<int> dist(n + 1, INF);
priority_queue<pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>>> pq; // {距离, 点}
dist[s] = 0;
pq.push({0, s});
while (!pq.empty()) {
    auto [d, u] = pq.top(); pq.pop();
    if (d > dist[u]) continue;      // 旧记录，跳过
    for (auto [v, w] : g[u]) {
        if (dist[u] + w < dist[v]) {
            dist[v] = dist[u] + w;
            pq.push({dist[v], v});
        }
    }
}
// 结果：dist[目标点]，仍为INF则不可达
```

### 3. 拓扑排序（Kahn算法，判断有向图是否有环）

```cpp
vector<int> in(n + 1);              // 入度，建图时算
queue<int> q;
for (int i = 1; i <= n; i++) if (in[i] == 0) q.push(i);
vector<int> topo;
while (!q.empty()) {
    int u = q.front(); q.pop();
    topo.push_back(u);
    for (int v : g[u]) if (--in[v] == 0) q.push(v);
}
// topo.size() == n 则有拓扑序（topo即答案）；否则图有环
```

---

## 十四、素数筛（批量判断 1~n 所有数，高频）

### 1. 埃氏筛 O(n log log n)

```cpp
bool isPrime[1000005];
void sieve(int n) {
    memset(isPrime, true, sizeof(isPrime));
    isPrime[0] = isPrime[1] = false;
    for (int i = 2; i * i <= n; i++) {
        if (isPrime[i]) {
            for (int j = i * i; j <= n; j += i) isPrime[j] = false;
        }
    }
}
```

### 2. 线性筛（欧拉筛）O(n)，同时得到素数表

```cpp
int primes[1000005], pc = 0;
bool notPrime[1000005];
void linearSieve(int n) {
    for (int i = 2; i <= n; i++) {
        if (!notPrime[i]) primes[pc++] = i;
        for (int j = 0; j < pc && i * primes[j] <= n; j++) {
            notPrime[i * primes[j]] = true;
            if (i % primes[j] == 0) break; // 核心：保证每个数只被最小质因子筛一次
        }
    }
}
```

---

## 十五、组合数取模（逆元，配合第五节 qpow）

> `C(n,k) % MOD`，MOD 为大质数（如 1e9+7），用费马小定理求逆元

```cpp
const long long MOD = 1e9 + 7;
long long fac[100005], invfac[100005];
// 先实现 qpow(a, b, mod)（见第五节）

// 预处理阶乘和阶乘逆元
fac[0] = 1;
for (int i = 1; i < 100005; i++) fac[i] = fac[i - 1] * i % MOD;
invfac[100004] = qpow(fac[100004], MOD - 2, MOD); // 费马小定理：a^(p-2) 是 a 的逆元
for (int i = 100004; i >= 1; i--) invfac[i - 1] = invfac[i] * i % MOD;

long long C(int n, int k) {
    if (k < 0 || k > n) return 0;
    return fac[n] * invfac[k] % MOD * invfac[n - k] % MOD;
}
// 单个逆元： inv(a) = qpow(a, MOD - 2, MOD)
```

---

## 十六、字符串算法：KMP 与哈希

### 1. KMP：求模式串 p 在文本串 t 中的出现次数（含重叠）

```cpp
vector<int> getNext(const string& p) {
    int m = p.size();
    vector<int> nxt(m, 0);
    for (int i = 1, j = 0; i < m; i++) {
        while (j > 0 && p[i] != p[j]) j = nxt[j - 1];
        if (p[i] == p[j]) j++;
        nxt[i] = j;
    }
    return nxt;
}
int kmpCount(const string& t, const string& p) {
    auto nxt = getNext(p);
    int ans = 0;
    for (int i = 0, j = 0; i < (int)t.size(); i++) {
        while (j > 0 && t[i] != p[j]) j = nxt[j - 1];
        if (t[i] == p[j]) j++;
        if (j == (int)p.size()) {
            ans++;                 // 匹配成功，此时起点 i-j+1
            j = nxt[j - 1];        // 允许重叠，继续匹配
        }
    }
    return ans;
}
```

### 2. 字符串哈希（重点理解）

**思想**：把字符串看成一个 `BASE` 进制的大整数，每个子串对应一个唯一数值。

例如 `"abc"`（用 ASCII 码算）：`97 * BASE² + 98 * BASE + 99`

**递推关系**（类似十进制左移补位）：

```cpp
// h[i] = 前 i 个字符的哈希值
// h[i] = h[i-1] * BASE + s[i-1]
```

十进制类比：前缀 `"12"` 后面接 `"3"`，整体左移一位再补个位：`12*10 + 3 = 123`

**O(1) 取子串哈希**：

```
区间 [l, r] 的哈希 = h[r] - h[l-1] * BASE^(r-l+1)
```

直觉：`h[r]` 是「前 r 位」，`h[l-1] * BASE^(len)` 把「前 l-1 位」整体左移 len 位，和 `h[r]` 对齐后相减，正好剩下 `[l, r]` 段。

十进制类比：取 `"12345"` 的第 3~4 位 `"34"`：
`1234 - 12 * 10² = 1234 - 1200 = 34` ✅

**碰撞**：哈希值存 `unsigned long long`，溢出自动等价于 mod 2⁶⁴。不同字符串撞同值的概率极低，是竞赛通用做法。

**用途**：
- O(1) 判断两子串是否相等：`getHash(l1,r1) == getHash(l2,r2)`
- 判断回文：原串和反转串的哈希相等
- 查找重复子串

```cpp
const int BASE = 131;              // 或 13331
unsigned long long h[100005], pw[100005]; // h[0]=0, pw[0]=1
for (int i = 1; i <= n; i++) {
    h[i] = h[i - 1] * BASE + s[i - 1]; // s下标0开始，h下标1开始
    pw[i] = pw[i - 1] * BASE;
}
// 子串 s[l-1..r-1] 的哈希（l,r从1开始）
unsigned long long getHash(int l, int r) {
    return h[r] - h[l - 1] * pw[r - l + 1];
}
// 判断子串[l1,r1]和[l2,r2]是否相等：getHash(l1,r1) == getHash(l2,r2)
```

---

## 十七、归并排序求逆序对（O(n log n)）

```cpp
long long cnt = 0;   // 逆序对个数
int tmp[100005];
void mergeSort(int a[], int l, int r) {
    if (l >= r) return;
    int mid = (l + r) / 2;
    mergeSort(a, l, mid);
    mergeSort(a, mid + 1, r);
    int i = l, j = mid + 1, k = l;
    while (i <= mid && j <= r) {
        if (a[i] <= a[j]) {
            tmp[k++] = a[i++];
        } else {
            tmp[k++] = a[j++];
            cnt += mid - i + 1;   // 关键：左半剩余元素都比 a[j] 大，全算逆序对
        }
    }
    while (i <= mid) tmp[k++] = a[i++];
    while (j <= r)   tmp[k++] = a[j++];
    for (i = l; i <= r; i++) a[i] = tmp[i];
}
```

---

## 十八、单调队列（滑动窗口最值，O(n)）

> 滑动窗口大小为 k，求每个窗口的最大值。核心：队内元素下标递增、值单调递减；窗口滑动时淘汰队首。

```cpp
deque<int> dq;                 // 存下标
vector<int> ans;
for (int i = 0; i < n; i++) {
    while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back(); // 保持递减
    dq.push_back(i);
    if (!dq.empty() && dq.front() + k <= i) dq.pop_front();    // 移出窗口
    if (i >= k - 1) ans.push_back(a[dq.front()]);              // 当前窗口最大值
}
// 求最小值：把 <= 换成 >= 即可
```

---

## 十九、动态规划 DP（高频）

> 通用套路四步：**① 状态定义 → ② 转移方程 → ③ 初始化 → ④ 答案**
> 想不出转移：先想"最后一步"怎么从更小的状态推来。

### 1. 01背包（每件物品取或不取，只能一次）

```cpp
// n件物品，背包容量V，w[i]重量 val[i]价值，求最大价值
int dp[1005] = {0};
for (int i = 1; i <= n; i++)
    for (int j = V; j >= w[i]; j--)   // 关键：倒序，保证每件只用一次
        dp[j] = max(dp[j], dp[j - w[i]] + val[i]);
// 答案：dp[V]
```

### 2. 完全背包（每件物品可无限取）

```cpp
for (int i = 1; i <= n; i++)
    for (int j = w[i]; j <= V; j++)   // 关键：正序，可重复取
        dp[j] = max(dp[j], dp[j - w[i]] + val[i]);
// 答案：dp[V]
```

> 记忆口诀：**01 倒序，完全正序**（一维滚动数组时）

### 3. 最长上升子序列 LIS（O(n log n)，贪心+二分）

```cpp
// d[len] = 长度为len的上升子序列末尾最小值（d单调递增）
vector<int> d;
for (int i = 0; i < n; i++) {
    auto it = lower_bound(d.begin(), d.end(), a[i]); // 严格上升
    if (it == d.end()) d.push_back(a[i]);
    else *it = a[i];
}
// 答案：d.size()
// 不降子序列：把 lower_bound 换成 upper_bound
```

### 4. 最长公共子序列 LCS（二维DP）

```cpp
vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        if (a[i - 1] == b[j - 1]) dp[i][j] = dp[i - 1][j - 1] + 1;
        else dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);
// 答案：dp[n][m]
```

### 5. 区间DP：合并相邻石子堆的最小代价

```cpp
// 每堆石子 w[i]，合并相邻两堆代价 = 两堆总和，求全部合并的最小总代价
int sum[505];  // 前缀和，sum[i] = w[1]+...+w[i]
int dp[505][505] = {};
for (int len = 2; len <= n; len++) {          // 区间长度从2开始
    for (int i = 1; i + len - 1 <= n; i++) {
        int j = i + len - 1;
        dp[i][j] = 1e9;
        for (int k = i; k < j; k++)           // 枚举分割点
            dp[i][j] = min(dp[i][j], dp[i][k] + dp[k + 1][j]);
        dp[i][j] += sum[j] - sum[i - 1];      // 最后一步：合并本次区间总代价
    }
}
// 答案：dp[1][n]
```
